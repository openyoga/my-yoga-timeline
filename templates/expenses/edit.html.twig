{% extends 'base.html.twig' %}

{% block title %}Edit Expense #{{ expense.id }}{% endblock %}

{% block body %}

{{ form_start(form) }}
{{ form_widget(form) }}
<div class="form-group">
  <button type="submit" class="btn btn-primary">Save</button>
  <a href="/expenses" class="btn btn-outline-primary">Cancel</a>
</div>
{{ form_end(form) }}

{% endblock %}
